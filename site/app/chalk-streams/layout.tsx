import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Chalk Streams',
  description: 'England holds 85% of the world s chalk streams rare ecosystems sometimes called the rainforest of the rivers yet 95% are in poor ecological condition. Over-abst',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
